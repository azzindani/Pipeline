//! `pipeline-re` — placeholder crate, layout locked at POC scaffold.
//!
//! See `PLAN.md` for the milestone that activates this crate.

/// Crate version, exposed for diagnostics.
pub const VERSION: &str = env!("CARGO_PKG_VERSION");
